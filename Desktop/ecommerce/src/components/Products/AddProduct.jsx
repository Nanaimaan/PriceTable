import React from 'react';

const AddProduct = () => {
    const {addProduct} = useContext(productContext);
    return (
        <div>
            
        </div>
    );
};

export default AddProduct;